diff --git a/working/G4C-INFRA-CERT-004/code/runtime_benchmark.py b/working/G4C-INFRA-CERT-004/code/runtime_benchmark.py
new file mode 100644
index 0000000..7f877ad
--- /dev/null
+++ b/working/G4C-INFRA-CERT-004/code/runtime_benchmark.py
@@ -0,0 +1,154 @@
+"""NON-SCIENTIFIC runtime certification on five fixed E3 manifest episodes.
+
+This harness exercises the generated optimized production coordinator helpers, unchanged
+workers, Linux transport, approved boundary, runtime checks, isolation and teardown. It
+never supplies query labels, never scores, and never creates a scientific run verdict.
+"""
+from __future__ import annotations
+import datetime, hashlib, importlib.util, json, pathlib, pickle, statistics, sys, time, traceback
+import numpy as np
+
+ROOT = pathlib.Path(__file__).resolve().parents[3]
+CERT = ROOT / "working/G4C-INFRA-CERT-004"
+BENCH = CERT
+COORD = CERT / "coordinator"
+OUT = CERT / "evidence/runtime_measurements.json"
+MANIFEST = ROOT / "working/gate4_episode_manifest.json"
+SELECTED = [0, 20, 30, 40, 50]  # B005, B025, B050, B100, B200; fixed trial 1
+EXPECTED_MANIFEST = "9d493d99f60098cb22fd781c51e2d2907d6831df72cb2524700293aa1a1bbf32"
+EXPECTED_RF = "5ffdd11f300532a14b3c2957a1ac77b748aa91c0a4d1a88f1ff0a3735dc75870"
+
+def utc():
+    return datetime.datetime.now(datetime.timezone.utc).isoformat()
+
+def sha(path):
+    with pathlib.Path(path).open("rb") as stream:
+        return hashlib.file_digest(stream, "sha256").hexdigest()
+
+def load_coordinator():
+    path = COORD / "execution_coordinator.py"
+    spec = importlib.util.spec_from_file_location("cert004_optimized_coordinator", path)
+    module = importlib.util.module_from_spec(spec)
+    assert spec and spec.loader
+    spec.loader.exec_module(module)
+    module.RUN = BENCH
+    return module
+
+def main():
+    if OUT.exists():
+        raise RuntimeError("Certification evidence already exists; no retry/overwrite")
+    overall_start = time.perf_counter(); wall_start = utc()
+    c = load_coordinator()
+    # Bind coordinator path globals explicitly; generated production code and worker bytes remain unchanged.
+    c.ROOT = ROOT
+    c.RUN = BENCH
+    c.MANIFEST = MANIFEST
+    fixed = []
+    for p in sorted(BENCH.rglob("*")):
+        if p.is_file() and not any(part in ("evidence", "predictions", "candidate_state", "results") for part in p.relative_to(BENCH).parts):
+            fixed.append({"path": p.relative_to(ROOT).as_posix(), "bytes": p.stat().st_size, "sha256": sha(p)})
+    c.STATIC = fixed
+    scans = []
+    original_static = c.static_integrity
+    def measured_static():
+        start = time.perf_counter(); began = utc()
+        original_static()
+        scans.append({"started_utc": began, "finished_utc": utc(), "seconds": time.perf_counter()-start})
+    c.static_integrity = measured_static
+
+    record = {
+        "designation": "NON-SCIENTIFIC / RUNTIME-CERTIFICATION ONLY",
+        "full_e3_run": False, "query_labels_transported": False, "query_labels_scored": False,
+        "selected_manifest_ordinals": [i+1 for i in SELECTED], "started_utc": wall_start,
+        "manifest_sha256": sha(MANIFEST),
+        "rf_source_path": str((ROOT/'working/baseline_artifacts/rf_final.pkl').resolve()),
+        "rf_source_sha256": sha(ROOT/'working/baseline_artifacts/rf_final.pkl'),
+        "phases": {}, "episodes": []
+    }
+    assert record["manifest_sha256"] == EXPECTED_MANIFEST
+    assert record["rf_source_sha256"] == EXPECTED_RF
+
+    start=time.perf_counter(); c.preexisting_integrity(); record["phases"]["coordinator_startup_integrity_seconds"]=time.perf_counter()-start
+    start=time.perf_counter(); time.sleep(5); record["phases"]["production_metadata_settle_seconds"]=time.perf_counter()-start
+    start=time.perf_counter(); c.install_pickle_dependency()
+    manifest=json.loads(MANIFEST.read_text())
+    with open(ROOT/'data/preprocessed/preprocessed_data.pkl','rb') as f:data=pickle.load(f)
+    x,y,ids=data['X_amsterdam'],data['y_amsterdam'],data['pixel_ids_amsterdam'];xm,ym=data['X_madrid'],data['y_madrid']
+    with np.load(ROOT/'working/baseline_artifacts/amsterdam_zero_shot.npz',allow_pickle=False) as a:
+        rf=a['y_ams_pred_zero'].copy();assert np.array_equal(a['y_amsterdam'],y) and np.array_equal(a['pixel_ids_amsterdam'],ids)
+    record["phases"]["data_and_comparator_load_seconds"]=time.perf_counter()-start
+
+    # Exact authoritative full-manifest RNG replay, as in production; no manifest mutation.
+    start=time.perf_counter();rng=np.random.default_rng(42)
+    for i,e in enumerate(manifest['episodes']):
+        assert rng.bit_generator.state==e['rng_before']
+        s=np.concatenate([rng.choice(np.flatnonzero(y==cl),e['budget'],replace=False) for cl in c.CLASSES])
+        assert np.array_equal(s,e['support_row_indices']) and rng.bit_generator.state==e['rng_after']
+    record["phases"]["manifest_rng_replay_seconds"]=time.perf_counter()-start
+
+    e=manifest['episodes'][0];s=np.asarray(e['support_row_indices']);q=np.setdiff1d(np.arange(len(y)),s)
+    probe={'X_support':x[s].tolist(),'y_support':y[s].tolist(),'support_ids':ids[s].tolist(),'X_query':x[q].tolist(),'query_ids':ids[q].tolist(),'parameters':{'mode':'preflight'}}
+    preflights=[]
+    for n in (1,2):
+        before=len(scans);start=time.perf_counter();z=c.call(probe,'preflight',f'benchmark_preflight_{n}')
+        preflights.append({"ordinal":n,"wall_seconds":time.perf_counter()-start,"boundary_seconds":z['boundary_evidence']['elapsed_seconds'],"integrity_scans":scans[before:]})
+        assert z['result']['status']=='PASS' and not z['result']['model_computation']
+    record["preflights"]=preflights
+
+    source_packet={'X_source':xm.tolist(),'y_source':ym.tolist(),'parameters':c.PARAMS}
+    before=len(scans);start=time.perf_counter();z=c.call(source_packet,'source','benchmark_source_worker');source_wall=time.perf_counter()-start
+    state=z['result']['source_state'];state_hash=z['result']['source_state_sha256']
+    record["source_worker"]={"wall_seconds":source_wall,"boundary_seconds":z['boundary_evidence']['elapsed_seconds'],"integrity_scans":scans[before:]}
+    c.save(BENCH/'candidate_state/source_state.json',{'source_state':state,'source_state_sha256':state_hash,'worker_peak_rss_kib':z['result']['peak_rss_kib']})
+
+    prior_finish=time.perf_counter()
+    for index in SELECTED:
+        e=manifest['episodes'][index];episode_start=time.perf_counter();episode_wall_start=utc();inter=episode_start-prior_finish
+        scan_before=len(scans);scan_start=time.perf_counter();c.static_integrity();predispatch_scan=time.perf_counter()-scan_start
+        packet_start=time.perf_counter();s=np.asarray(e['support_row_indices'],dtype=np.int64);q=np.setdiff1d(np.arange(len(y)),s,assume_unique=True)
+        assert np.array_equal(ids[s],np.asarray(e['support_pixel_ids']))
+        packet={'X_support':x[s].tolist(),'y_support':y[s].tolist(),'support_ids':ids[s].tolist(),'X_query':x[q].tolist(),'query_ids':ids[q].tolist(),'source_state':state,'parameters':{'episode_id':e['episode_id'],'budget':e['budget'],'trial':e['trial'],'classes':[1,2,3,4],'candidate':'E3'}}
+        packet_seconds=time.perf_counter()-packet_start
+        call_start=time.perf_counter();z=c.call(packet,'episode',f'benchmark_worker_{index+1:02d}');call_seconds=time.perf_counter()-call_start
+        r=z['result'];assert r['label_access']=={'source_state_only':True,'current_support_labels':len(s),'query_labels':0,'other_episode_labels':0}
+        assert r['fresh_state_probe'] and r['source_state_sha256']==state_hash
+        pred={k:np.asarray(v,dtype=np.int64) for k,v in r['predictions'].items()};pred['frozen_rf']=rf[q]
+        assert np.array_equal(pred['rf_reproduced'],pred['frozen_rf'])
+        with np.load(ROOT/'working/G4C-E3-001/predictions'/f"{e['episode_id']}.npz",allow_pickle=False) as old:
+            assert np.array_equal(old['query_indices'],q) and np.array_equal(old['query_ids'],ids[q])
+            assert np.array_equal(old['raw_prototype'],pred['raw_prototype']) and np.array_equal(old['candidate'],pred['candidate'])
+        serialize_start=time.perf_counter();path=BENCH/'predictions'/f"NON_SCIENTIFIC_{e['episode_id']}.npz"
+        with path.open('xb') as f:np.savez_compressed(f,query_indices=q,query_ids=ids[q],**pred)
+        serialize_seconds=time.perf_counter()-serialize_start
+        finish=time.perf_counter();prior_finish=finish
+        infra=z.get('infrastructure_io');boundary=z['boundary_evidence'];call_scans=scans[scan_before+1:]
+        record['episodes'].append({
+            'episode_id':e['episode_id'],'manifest_ordinal':index+1,'budget':e['budget'],'trial':e['trial'],
+            'started_utc':episode_wall_start,'finished_utc':utc(),'inter_episode_seconds':inter,
+            'predispatch_integrity_seconds':predispatch_scan,'call_integrity_scans':call_scans,
+            'packet_construction_seconds':packet_seconds,'call_wall_seconds':call_seconds,
+            'boundary_worker_supervisor_seconds':boundary['elapsed_seconds'],
+            'call_outside_boundary_seconds':call_seconds-boundary['elapsed_seconds'],
+            'native_cache_verification_seconds':infra['seconds'],'native_rf_path':infra['path'],'native_rf_created':infra['created'],
+            'prediction_serialization_seconds':serialize_seconds,'total_episode_cycle_seconds':finish-episode_start,
+            'teardown_pass':boundary['teardown_pass'],'final_survivors':boundary['lifecycle']['final_survivors'],
+            'transport_returncode':boundary['returncode'],'transport_reason':boundary['reason'],
+            'worker_peak_rss_kib':r['peak_rss_kib'],'warnings':r['warnings'],
+            'support_row_indices_sha256':e['support_row_indices_sha256'],'query_row_indices_sha256':e['query_row_indices_sha256'],
+            'query_pixel_ids_sha256':e['query_pixel_ids_sha256'],'packet_sha256':boundary['packet_sha256'],
+            'prediction_artifact':path.relative_to(ROOT).as_posix(),'prediction_sha256':sha(path)
+        })
+
+    cycles=[e['total_episode_cycle_seconds'] for e in record['episodes']]
+    record['all_integrity_scans']=scans
+    record['finished_utc']=utc();record['total_benchmark_wall_seconds']=time.perf_counter()-overall_start
+    record['episode_statistics']={'count':len(cycles),'mean_seconds':statistics.mean(cycles),'median_seconds':statistics.median(cycles),'maximum_seconds':max(cycles),'minimum_seconds':min(cycles)}
+    OUT.write_text(json.dumps(record,indent=2,allow_nan=False)+'\n')
+    print(json.dumps(record['episode_statistics'],indent=2));print('TOTAL',record['total_benchmark_wall_seconds'])
+
+if __name__=='__main__':
+    try:main()
+    except BaseException:
+        p=CERT/'evidence/BENCHMARK_STOP_REASON.md'
+        if not p.exists():p.write_text('# Runtime benchmark STOP\n\n```\n'+traceback.format_exc()+'\n```\n')
+        raise
diff --git a/working/G4C-E3-001/coordinator/execution_coordinator.py b/working/G4C-INFRA-CERT-004/coordinator/execution_coordinator.py
index 8fda10c..1eef082 100644
--- a/working/G4C-E3-001/coordinator/execution_coordinator.py
+++ b/working/G4C-INFRA-CERT-004/coordinator/execution_coordinator.py
@@ -35,11 +35,13 @@ def install_pickle_dependency():
  module=importlib.util.module_from_spec(spec);sys.modules['working']=package;sys.modules['working.minimal_standard_scaler']=module;setattr(package,'minimal_standard_scaler',module);before=list(sys.path);spec.loader.exec_module(module);assert list(sys.path)==before and module.StandardScaler.__module__=='working.minimal_standard_scaler'
 def original_clean():
  return not subprocess.check_output(['git','status','--short','--','original/'],cwd=ROOT) and not subprocess.check_output(['git','diff','--','original/'],cwd=ROOT)
+def signature_hex(path):
+ with pathlib.Path(path).open('rb') as stream:return stream.read(16).hex()
 def preexisting_integrity():
  cp=json.loads((ROOT/'working/G4C-PROVENANCE-CHECKPOINT-002/file_fingerprints.json').read_text())
  bad=[];metadata=[]
  for rel,v in cp.items():
-  p=ROOT/rel;s=p.stat();cur={'sha256':sha(p),'bytes':s.st_size,'mtime_ns':s.st_mtime_ns,'file_id':s.st_ino,'attributes':s.st_file_attributes,'signature_hex':p.read_bytes()[:16].hex(),'regular':p.is_file(),'links':s.st_nlink}
+  p=ROOT/rel;s=p.stat();cur={'sha256':sha(p),'bytes':s.st_size,'mtime_ns':s.st_mtime_ns,'file_id':s.st_ino,'attributes':s.st_file_attributes,'signature_hex':signature_hex(p),'regular':p.is_file(),'links':s.st_nlink}
   if cur!=v:
    if rel=='working/Open Notebook.onetoc2' and all(cur[k]==v[k] for k in ('file_id','attributes','signature_hex','regular','links')) and cur['signature_hex']=='a12fff43d9ef764c9ee210ea5722765f':metadata.append({'before':v,'after':cur})
    else:bad.append(rel)
diff --git a/working/G4C-E2-008/coordinator/g4c_linux_call.py b/working/G4C-INFRA-CERT-004/coordinator/g4c_linux_call.py
index 920a995..08d65e9 100644
--- a/working/G4C-E2-008/coordinator/g4c_linux_call.py
+++ b/working/G4C-INFRA-CERT-004/coordinator/g4c_linux_call.py
@@ -2,11 +2,22 @@
 import argparse,json,traceback
 from pathlib import Path
 from g4c_boundary import run
+from immutable_io import RF_SHA256, stage_frozen_file
 p=argparse.ArgumentParser();p.add_argument('packet',type=Path);p.add_argument('worker',type=Path);p.add_argument('role');p.add_argument('output',type=Path);p.add_argument('--rf',type=Path);a=p.parse_args()
 assert not a.output.exists()
 try:
-    packet=json.loads(a.packet.read_text());raw,evidence=run(packet,a.worker.read_bytes(),a.role,rf_path=a.rf)
-    result={'result':json.loads(raw),'boundary_evidence':evidence}
+    packet=json.loads(a.packet.read_text())
+    native_record=None
+    if a.rf is not None:
+        if a.role!='episode' or packet['parameters']['candidate'] not in ('E3','E5'):
+            raise RuntimeError('RF supplied to an unauthorized worker role')
+        config=json.loads(Path(__file__).with_name('native_cache.json').read_text())
+        if set(config)!={'directory','rf_sha256'} or config['rf_sha256']!=RF_SHA256:
+            raise RuntimeError('Invalid native cache configuration')
+        native_record=stage_frozen_file(a.rf,Path(config['directory']),RF_SHA256)
+        a.rf=Path(native_record['path'])
+    raw,evidence=run(packet,a.worker.read_bytes(),a.role,rf_path=a.rf)
+    result={'result':json.loads(raw),'boundary_evidence':evidence,'infrastructure_io':native_record}
     with a.output.open('x') as f:json.dump(result,f,separators=(',',':'));f.write('\n')
     print(json.dumps({'status':'PASS','role':a.role,'elapsed_seconds':evidence['elapsed_seconds']}))
 except BaseException:
