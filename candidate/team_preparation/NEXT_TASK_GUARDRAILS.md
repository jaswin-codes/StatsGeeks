# Guardrails for any separately authorized future task

This document is not authorization to optimize. No overnight ML discovery started in preparation.

1. **Never modify** `candidate/SUBMISSION_SAFETY_SNAPSHOT/` or `candidate/SUBMISSION_SAFETY_SNAPSHOT.zip`, including indexes, manifests or checklist. Never run a notebook from that directory. The clean sealed ZIP is authoritative for the 13-file intended payload; preserve the seven unsealed background OneNote files in place rather than repairing the frozen directory silently.
2. Verify `SNAPSHOT_SEAL.json` and the protected original artifacts before any future work. The safety ZIP SHA256 is `8138e6ba7a55467de48e8120b7f2f49a4a66410a5c202cabd954a4174596f6ea`.
3. Keep EXP-010 code/state/results unchanged. Preserve current Coordinate_RF/ASTRA/EXP-F implementations, locks, predictions and original runs. New alternatives, if independently authorized, must have new names/output directories and cannot replace frozen files.
4. Do not alter competitor repositories, fetch new history without separate scope, import their code into our implementation, or copy their data/predictions/documents/figures/workflows into submission material.
5. Existing label-bearing evaluation/development files are not fitting permission. A future protocol must define support, development, query and allowed unlabelled-target access before scoring or selection.
6. Do not reuse historical final query feedback as a pristine test, claim new seeds create new cities, or change the incumbent based on a newly inspected final audit without a separately justified selection protocol.
7. Preserve all negative/blocked experiments and provenance. Do not rerun completed frozen optimization or erase failed logs.
8. Resolve the **missing final-method notebook and stale organizer presentation** in a separate new deliverable package. Do not relabel baseline notebooks as the current solution. The snapshot is a protected fallback, not a certified complete submission.
9. Clarify full-pool/coordinate eligibility, deadline, presentation duration, attachment limit and any word-count convention. The known destination is `uctstatsgeekssociety@uct.ac.za`; no email has been sent.
10. Current publication inference/evidence replay is tested. Its optional source rebuild has a separately reported missing scaler-module dependency in the archive; do not claim every optional replay is already standalone or patch the old archive silently.

**Current status:** preservation permits future work to be isolated safely, but submission completeness is not certified. More optimization is not automatically the highest-priority next action. Stop until the separately scoped next task is supplied.
