# Additional fixed robustness evaluation (declared after random results)

Regularized covariance shows substantial random-protocol gains. Apply the identical EXP-D split and 10-key buffer to ALL THREE predeclared EXP-F metrics, not only the best arm. No new metric, ridge, shrinkage, seed or buffer selection. This follow-up is motivated by observed results, explicitly exploratory, and cannot independently confirm an audit-identified winner. No deployment selection or promotion from audit results.

Organiser Notebook2 section2.3 confirms px_key/py_key are rounded UTM coordinates divided by30m. Thus the fixed 10-key exclusion is approximately300m; nearest cross-split query and support grid indices differ by at least11 keys for this data. Geometry is used only in evaluation sampling. Official random headline remains unchanged.

Run `python -I -B candidate/experiments_2h/spatial_f.py`, then same command with `--repeat`. No changes to previously executed run.py or PLAN.md.
