# Datasets

Copy the organiser datasets into this folder before running the notebooks.

Required files:

- madrid_train.parquet
- amsterdam_data.parquet

These files are intentionally excluded from Git because they exceed GitHub's file size limit.
